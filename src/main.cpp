#include "graf.h"
#include "dijkstra.h"
#include "scena.h"
#include "testy.h"
#include <chrono>

int main() {
/// Zmienne do aktywowania funkcji
bool _menu = false;
bool _test = true;

pokaz(_menu);
test(_test);
//testy();

	return 0;
}